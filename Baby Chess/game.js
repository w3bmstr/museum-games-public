// Go game module (starter)
// Copilot will fill in the rules once it sees this structure.

export function createGame() {
    return {
        init(layers) {},
        update() {},
        drawBoard(ctx) {},
        drawPieces(ctx) {},
        drawUI(ctx) {},
        handleInput(type, event) {},
        getAIMove(state) {},
        restart() {},
        undo() {}
    };
}
